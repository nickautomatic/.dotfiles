- name: Symlink various dotfiles from home directory
  file:
    src: "~/.dotfiles/{{ item }}"
    dest: "~/{{ item }}"
    state: link
  with_items:
  - .asdfrc
  - .gemrc
  - .gitignore_global
  - .tigrc
  - .tmux.conf
  - .vimrc
  tags: [ symlinks ]
